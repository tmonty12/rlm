FROM python:3.12-slim

COPY . /app
WORKDIR /app

RUN pip install --no-cache-dir -e . fastapi uvicorn requests

EXPOSE 9090

CMD ["python3", "-m", "uvicorn", "rlm_dynamo.sandbox:app", "--host", "0.0.0.0", "--port", "9090"]
